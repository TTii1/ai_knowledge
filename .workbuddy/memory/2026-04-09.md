# 2026-04-09 工作日志

## KnowledgeForge 项目 - Phase 0 完成

### 完成事项
1. 安装 uv 包管理器 (0.11.6)
2. 初始化项目: `uv init --name knowledge-forge`
3. 配置 pyproject.toml 完整依赖 (150+ packages, 含 dev 依赖)
4. 创建虚拟环境 (.venv, Python 3.13.9)
5. 使用阿里云 PyPI 镜像安装所有依赖 (pypi.org 直连不通)
6. 创建完整项目目录结构 (82 文件, 10043 行)
7. 实现所有核心模块骨架:
   - config/: Pydantic Settings + 日志
   - document/: 4种解析器 + 2种切分器 + 处理流水线
   - embedding/: OpenAI + BGE 抽象层
   - storage/: Milvus/PG/Redis/文件存储
   - rag/: Query改写/多路检索/RRF融合/Reranker/上下文构建/流式生成
   - conversation/: 会话管理 + 滑动窗口记忆 + 指代消解
   - mcp_server/: 4个标准MCP工具
   - evaluation/: 指标/数据集/报告
   - tasks/: Celery配置 + 文档处理任务
   - api/: 文档/知识库/问答/管理 4组API
8. 编写单元测试: 11 tests, all passed
9. Docker Compose 编排文件 (Milvus+etcd+minio+PG+Redis)
10. Git commit: c09a29a

### 注意事项
- pypi.org 直连不通，需使用阿里云镜像
- Python 使用系统 3.13.9（uv 下载 3.11 因网络问题失败）
- git push 被跳过，需手动推送

## KnowledgeForge 项目 - Phase 1 完成

### 完成事项
1. 完善 Markdown 解析器：使用状态机追踪代码块，代码块内 `#` 不误判为标题
2. 修复 Word 解析器：兼容中英文标题样式（标题1/Heading 1），添加 _parse_heading_level 函数
3. 修复 Chunk overlap 问题：overlap 存入 context_before/context_after，不再拼入 content
4. 实现完整的 MetadataStore：SQLAlchemy async ORM，知识库/文档表模型 + CRUD
5. 完善依赖注入模块：统一管理 VectorStore/MetadataStore/Embedding/Pipeline 实例
6. Pipeline 新增 process_and_store 方法：串联解析→切分→向量化→存储
7. 完善文档 API：文件保存+Celery任务触发+列表/详情/删除/重索引全接口
8. 完善知识库 API：创建/列表/详情/删除 CRUD，含参数验证
9. 完善 Celery 任务：完整流水线+重试(3次)+错误状态更新，修复 asyncio 事件循环冲突
10. VectorStore 新增 delete_by_document/delete_by_knowledge_base 方法
11. 应用 lifespan：初始化 PG/Redis/Milvus，自动创建 default 知识库
12. 健康检查增强：返回各服务连接状态
13. 添加 aiosqlite 依赖（SQLite 异步测试支持）
14. 32 个单元测试全部通过（含 12 个 MetadataStore CRUD 测试）
15. Git commit: 2981592

### 关键修复
- CacheStore 缺少 Optional 导入（Python 3.13 严格检查）
- SQLite 不支持 pool_size/max_overflow（MetadataStore 动态判断数据库类型）
- datetime.utcnow() 弃用（改为 datetime.now(timezone.utc)）

## KnowledgeForge 项目 - Phase 2 完成

### 完成事项
1. 新增 RAGEngine 问答引擎：串联 Query改写→多路召回→Rerank→上下文构建→LLM生成
2. 完善 QueryRewriter：安全降级（无API Key跳过改写）、NONE策略、指代消解+LLM改写+HyDE+分解
3. 完善 VectorRetriever：异常安全降级、适配 VectorStore.search 字典格式
4. 完善 BM25Retriever：延迟加载依赖、从 VectorStore 构建 BM25 索引
5. 完善 HybridRetriever：异步并行多路检索(asyncio.gather)、异常安全
6. 完善 Reranker：异步执行(run_in_executor)、延迟加载、降级跳过
7. 完善 ContextBuilder：tiktoken降级为字符估算、Token预算控制、来源注入
8. 完善 Generator：API Key安全降级、is_available属性、降级回答
9. 新增 ConversationMemory 对话记忆管理：InMemory+Redis双存储、滑动窗口策略
10. 完善 Chat API：SSE流式输出、完整RAG串联、会话CRUD、来源引用
11. 更新依赖注入：RAGEngine/Retriever/Reranker/Generator/Memory 统一管理
12. FlagEmbedding 改为可选依赖（reranker extra）
13. 61 个单元测试全部通过（Phase 1: 32 → Phase 2: 61）
14. Git commit: 24f7d14
